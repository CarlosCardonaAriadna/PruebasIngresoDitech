CREATE DATABASE pruebaditech;

CREATE TABLE CIUDAD (  CODIGO int PRIMARY KEY NOT NULL ,
DESCRIPCION varchar (50)    
);

INSERT INTO CIUDAD (CODIGO, DESCRIPCION)
VALUES
	(1,'Bogotá'),
	(2, 'Cali');


CREATE TABLE VENDEDOR (  CODIGO int PRIMARY KEY NOT NULL ,
NOMBRE varchar (50) NOT NULL,
APELLIDO varchar (50) NOT NULL,
NUMERO_IDENTIFICACION varchar (150) NOT NULL,
CODIGO_CIUDAD int NOT NULL
);


alter table VENDEDOR
add constraint VENDEDOR_CIUDAD
foreign key (CODIGO_CIUDAD) references CIUDAD (CODIGO);



